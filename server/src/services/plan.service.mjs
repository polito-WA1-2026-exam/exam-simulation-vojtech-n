import * as PlanModel from '../models/plan.model.mjs'

export async function addCourse(studentId, courseIds) {
  // const currentStudyPlanCourses = getStudyPlanCourses(studentId);

  // Compare current enrolled courses vs. new study plan
}

export async function removeCourse(studentId, courseIds) {

}